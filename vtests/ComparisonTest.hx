class ComparisonTest {
    static function main() {
        var a = 10;
        var b = 20;
        
        if (a < b) {
            trace("a is less than b");
        }
        
        if (a == 10) {
            trace("a equals 10");
        }
        
        if (b != a) {
            trace("b is not equal to a");
        }
    }
}