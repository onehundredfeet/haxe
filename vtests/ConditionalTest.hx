class ConditionalTest {
    static function main() {
        var x = 10;
        if (x > 5) {
            trace("x is greater than 5");
        } else {
            trace("x is less than or equal to 5");
        }
    }
}